# Si backend y frontend estan en el mismo dominio, usar /api.
# Si la API esta separada, usar https://api.tu-dominio.cl/api.
VITE_API_URL=/api

VITE_ALLOW_PUBLIC_REGISTRATION=false
VITE_DEMO_MODE=false
